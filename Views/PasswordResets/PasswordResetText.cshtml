@model sample_rails_app_8th_edNT.Models.User
@{
    // You may need to pass the reset token and email from the controller
    var resetUrl = $"https://{Context.Request.Host}/PasswordResets/Edit?email={Model.Email}&token={Model.ResetToken}";
}

Hello @Model.Name,

You requested a password reset. Click the link below to reset your password:

@resetUrl

If you did not request a password reset, please ignore this email.
